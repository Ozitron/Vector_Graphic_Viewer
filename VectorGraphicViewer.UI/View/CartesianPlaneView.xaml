<Window x:Class="VectorGraphicViewer.UI.View.CartesianPlaneView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VectorGraphicViewer.UI.View"
        xmlns:viewModel="clr-namespace:VectorGraphicViewer.UI.ViewModel"
        mc:Ignorable="d"
        Title="CartesianPlaneView" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:CartesianPlaneViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Content="Click"  
                Height="23"  
                Name="btnClick"  
                Command="{Binding ButtonCommand}"  
                CommandParameter="Hai" />
        <Label Grid.Row="1" x:Name="label" FontSize="30"  HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <Grid Grid.Row="1" Name="cartesianGrid">
            <ItemsControl ItemsSource="{Binding HelloString}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Grid>
</Window>
