<Window x:Class="VectorGraphicViewer.UI.View.CartesianPlaneView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VectorGraphicViewer.UI.View"
        xmlns:viewModel="clr-namespace:VectorGraphicViewer.UI.ViewModel"
        mc:Ignorable="d"
        Title="CartesianPlaneView" Height="450" Width="800">
    <Window.DataContext>
        <viewModel:CartesianPlaneViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <Button Content="Click" Height="20"  Command="{Binding ReadCommand}"  />
        <Button Grid.Row="1" Content="Clear" Height="20"  Command="{Binding ClearCommand}"  />
        <Grid Grid.Row="2" Name="cartesianGrid">
            <ItemsControl ItemsSource="{Binding Shapes, UpdateSourceTrigger=PropertyChanged}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas Margin="5,5,5,5" Background="Azure"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>
        </Grid>
    </Grid>
</Window>
